@page
@model LEADRS_NEW_DBFirst.Pages.DRE.Pages.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DreCasePage[0].PageName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DreCasePage[0].Url)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DreCasePage[0].PageOrder)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DreCasePage[0].Active)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.DreCasePage) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PageName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Url)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PageOrder)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Active)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DreCasePageId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DreCasePageId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DreCasePageId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
