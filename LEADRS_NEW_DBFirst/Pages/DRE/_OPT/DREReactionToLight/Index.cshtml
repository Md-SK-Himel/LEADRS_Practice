@page
@model LEADRS_NEW_DBFirst.Pages.DRE._OPT.DREReactionToLight.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ReactionToLight[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReactionToLight[0].Active)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ReactionToLight) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Active)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ReactionToLightId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ReactionToLightId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ReactionToLightId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
