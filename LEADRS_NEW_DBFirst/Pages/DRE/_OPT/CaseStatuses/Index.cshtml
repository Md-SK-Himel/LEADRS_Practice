@page
@model LEADRS_NEW_DBFirst.Pages.DRE._OPT.CaseStatuses.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DRECaseStatus[0].StatusName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DRECaseStatus[0].Active)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.DRECaseStatus) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StatusName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Active)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DRECaseStatusId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DRECaseStatusId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DRECaseStatusId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
