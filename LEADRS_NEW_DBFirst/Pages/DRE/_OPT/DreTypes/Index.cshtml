@page
@model LEADRS_NEW_DBFirst.Pages.DRE._OPT.DreTypes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TblOptDreType[0].TypeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TblOptDreType[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TblOptDreType[0].Active)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.TblOptDreType) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TypeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Active)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DretypeId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DretypeId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DretypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
